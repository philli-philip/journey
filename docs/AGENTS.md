# Agents

Only run lint, checking scripts when explicitly requested.

### random ID

instead off UUIDs use the randomID() in @shared/randomID.ts. If you need more collision avoidance use a higher number like 10 or 16.
